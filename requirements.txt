fastapi>=0.140.7
# Starlette is FastAPI's ASGI core and is pulled in transitively, but
# FastAPI's own floor (>=0.46) still permits versions with known advisories
# (PYSEC-2026-161/248/249/2280/2281). Vercel resolves this file fresh at
# build time with no lockfile, so the floor has to be stated here too.
starlette>=1.3.1
SQLAlchemy==2.0.29
pandas==2.2.3
numpy==1.26.4
PyYAML==6.0.1
python-dotenv>=1.0.0
pydantic>=2.13.4
# httpx is imported at module load time by backend.services.update_service
# (the GitHub Releases probe). Vercel installs from requirements.txt, not
# pyproject.toml, so it must be listed here even though poetry already pins it.
httpx>=0.28.0
