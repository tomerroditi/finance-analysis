# Data Flow

For a full interactive diagram of how data moves through the system — from external sources through scraping, processing, storage, analytics, and the frontend — visit the **Data Flow** page in the app:

**Route:** `/data-flow` (accessible from the sidebar, below Settings)

**Source code:** `frontend/src/components/dataflow/`
